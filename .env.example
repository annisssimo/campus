DATABASE_URL=postgresql://postgres:postgres@localhost:5432/todo?schema=public
# Must be at least 32 characters (validated on startup)
JWT_SECRET=change-me-in-production-use-long-random-string
JWT_EXPIRES_IN=7d
PORT=3000
NODE_ENV=development
# Comma-separated origins, or * for any (dev). Example: http://localhost:3000,https://app.example.com
CORS_ORIGIN=*
